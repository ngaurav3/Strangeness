2026-03-10
Iteration 6

# Reviewer Report

Author: Yen-Jie Lee and OpenAI

## Scope

This review checks consistency between:

- the analyzer-side Markdown status notes in the workspace
- the synced git/Overleaf copy under `overleaf_repo_git/`
- the active analysis note source
- the active workspace macros and current ROOT outputs

The main focus of this pass is unfolding documentation consistency.

## Findings

### 1. High: the main analysis note still contains one stale sentence describing the old proxy `dNch/deta` response, and it now contradicts both the current macros and the note's own new validation caption

Relevant files:

- `overleaf_repo_git/main.tex:481-487`
- `overleaf_repo_git/main.tex:372-376`
- `KtoPiAnalysis.cpp:983-989`
- `KtoPiAnalysis.cpp:1335-1340`
- `runDNdEtaUnfolding_BayesSVD.C:443-477`
- `report/20260310_dndeta_unfolding_update.md:37-49`

What is inconsistent:

- In the dedicated `K/pi` vs `dN_{ch}/d\eta` results section, the note still says
  the response is `R(dN_{ch}/d\eta_true, N_{tag}^{ch,reco})`.
- But the current macro implementation no longer does that.
- The active code now builds and unfolds with reco-side `dN_{ch}/d\eta` spectra and
  reco-side `dN_{ch}/d\eta` response axes.
- The newer refolding-validation caption in the same note already describes the
  corrected same-variable implementation.

Why this matters:

- This is now an internal contradiction inside the note itself.
- A reader can reach opposite conclusions depending on whether they read the
  results section text or the refolding-validation caption.

Bottom line:

- The `dN_{ch}/d\eta` results section still needs one documentation fix to match
  the actual workspace macro chain.

### 2. High: the iteration 4 unfolding-validation Markdown note is stale, and the workspace copy and synced git/Overleaf copy are stale in different ways

Relevant files:

- `report/20260310_iteration4_unfolding_validation_status.md:9-13`
- `overleaf_repo_git/report/20260310_iteration4_unfolding_validation_status.md:9-13`
- `report/20260310_dndeta_unfolding_update.md:93-116`

What is inconsistent:

- The workspace iteration 4 status note reports `dNch/deta` refolding RMS values
  around `0.456`.
- The synced git/Overleaf copy reports different `dNch/deta` refolding RMS values
  around `0.31-0.40`.
- The newer analyzer update note reports the current post-fix values
  `0.01724` and `0.03401`.

What I verified:

- The current ROOT file
  `output/systematics_20260306_dndeta/nominal_unfold_dndeta.root`
  matches the newer update note, not either iteration 4 status note.
- The current output also matches the updated refolding language now present in
  `main.tex`.

Why this matters:

- There are now three incompatible analyzer summaries for the same validation
  question.
- Two of them are stale, and the git/Overleaf copy is stale in a different way
  from the workspace copy.

Bottom line:

- The iteration 4 status note should be marked superseded everywhere, or
  refreshed to the current `dN_{ch}/d\eta` numbers.

### 3. Medium: the iteration 5 parameter-choice Markdown note and the analysis note are not using the same rule for the nominal Bayes choice

Relevant files:

- `report/20260310_iteration5_parameter_choice.md:3-7`
- `study_bayes_parameter_selection.py:18-21`
- `study_bayes_parameter_selection.py:204-229`
- `study_bayes_parameter_selection.py:289-296`
- `overleaf_repo_git/main.tex:378-410`
- `overleaf_repo_git/main.tex:488`

What is inconsistent:

- The analyzer iteration 5 note says there is no passing Bayes iteration for
  either `Ntag` or `dNch/deta`.
- The current analysis note still states that the `Ntag` iteration scan supports
  retaining `nIter=1` as the nominal setting.
- The workspace parameter-selection script really does implement the stricter
  bottom-line rule and writes out the "no passing iteration" summary.

Why this matters:

- Right now the Markdown status note and the main analysis note are answering
  the same parameter-choice question with different criteria and different
  conclusions.
- That is acceptable only if one is explicitly provisional. It is not acceptable
  if both are presented as current status.

Bottom line:

- The analysis team needs to decide whether iteration 5 is only an exploratory
  reviewer-style study or whether it replaces the older "monotonic scan
  therefore keep `nIter=1`" rule in the note.

### 4. Low: the iteration 5 parameter-choice summary has an internal off-by-one bug

Relevant files:

- `study_bayes_parameter_selection.py:18`
- `study_bayes_parameter_selection.py:295-296`
- `report/20260310_iteration5_parameter_choice.md:6-7`

What is happening:

- The study scans `nIter = 1..6`.
- But the generated Markdown summary says "no passing iteration in 1-5".

Why this matters:

- This is a reporting bug, not a physics bug.
- It still makes the summary self-inconsistent and should be corrected before it
  is cited elsewhere.

## What now looks consistent

The following items now match between the active code and the active note:

- the `dN_{ch}/d\eta` refolding-validation caption in the note matches the
  current same-variable reco/truth implementation
- the current `dN_{ch}/d\eta` update note matches the active workspace macros
- the current `dN_{ch}/d\eta` update note matches the verified current ROOT
  outputs for refolding and closure

## Assumptions and limits

- I treated `overleaf_repo_git/` as the synced git/Overleaf view requested in
  the review.
- I treated the active workspace macros and current ROOT outputs as authoritative
  for the present implementation.
- This was a consistency review only; no new analysis jobs were launched in this
  pass.

## Overall review status

The active code and the newer `dN_{ch}/d\eta` analyzer update are largely
aligned.

The remaining consistency problems are documentation and status-tracking issues:

- one stale `dN_{ch}/d\eta` response-definition sentence remains in the main note
- the iteration 4 Markdown status note is stale in both workspace and synced
  git/Overleaf copies
- the iteration 5 parameter-choice note has not yet been reconciled with the
  nominal parameter statement in the main note
