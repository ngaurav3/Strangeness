2026-03-11
Iteration 8

# Reviewer Report

Author: Yen-Jie Lee and OpenAI

## Scope

This pass is a careful note audit focused on:

- wording quality
- reference hygiene
- whether cited figure/section references actually resolve
- whether the note wording matches the current implemented workflow

## Findings

### 1. High: one stale workflow sentence still describes the old `dNch/deta` response definition

Relevant files:

- `overleaf_repo_git/main.tex:255-259`
- `overleaf_repo_git/main.tex:378-381`
- `overleaf_repo_git/main.tex:492-505`

What is wrong:

- In the workflow summary, the note still says the `dN_{ch}/d\eta` unfolding uses
  `x = true dN_{ch}/d\eta` and `y = reco N_{tag}^{ch}`.
- But the later refolding-validation text and the dedicated results subsection
  now correctly describe the same-variable response
  `R(dN_{ch}/d\eta_{\mathrm{true}}, dN_{ch}/d\eta_{\mathrm{reco}})`.

Why this matters:

- The note now contains two incompatible descriptions of the same unfolding
  chain.
- A reader who only reads the workflow section will still learn the wrong
  implementation.

Suggested fix:

- Rewrite `main.tex:255-259` to match the later corrected wording:
  the reco-side observable is built from charged `RecoGoodTrack==1` tracks in
  `|\eta|<0.5` with no PID requirement and no extra `pT` threshold, and the
  response is same-variable on reco and truth.

### 2. High: the `dNch/deta` refolding caption says the lower panels are shown on `0.0-1.2`, but the active plotting macro still draws them on `0.90-1.10`

Relevant files:

- `overleaf_repo_git/main.tex:378`
- `make_unfolding_validation_plots.py:123-124`
- `make_unfolding_validation_plots.py:386-398`

What is wrong:

- The note caption now says the lower panels are shown on the full `0.0` to
  `1.2` range to expose the tail behavior.
- But the active figure-generation script still hard-codes
  `SetMinimum(0.90)` and `SetMaximum(1.10)` for the ratio pads, including the
  `DNdEtaUnfolding_RefoldingValidation` figure.

Why this matters:

- Either the caption is wrong, or the checked-in plotting macro is stale with
  respect to the figure currently shown in the note.
- In either case, the note-to-code correspondence is not yet clean.

Suggested fix:

- Make the plotting macro match the caption if the full-range view is the new
  intended presentation.
- Otherwise, revert the caption to describe the actual plotted range.

### 3. Medium: several figure and section references are still hard-coded as raw numbers instead of label-based references

Relevant files:

- `overleaf_repo_git/main.tex:243`
- `overleaf_repo_git/main.tex:285`
- `overleaf_repo_git/main.tex:294-299`
- `overleaf_repo_git/main.tex:381`

What is wrong:

- The note still uses literal references such as:
  - `Section~3.1`
  - `Section~3.2`
  - `Figs.~2--3`
  - `Figure~2`
  - `Figure~3`
  - `Figure~13`

Why this matters:

- These are correct today, but they are brittle.
- They will silently become wrong when figure order or section structure changes.

Suggested fix:

- Replace these raw numeric references with `\label`/`\ref`-based references.
- If a figure does not currently have a label, add one and reference it through
  LaTeX rather than by hard-coded number.

### 4. Medium: the `p/pi` results wording still reads as if the `dNch/deta` branch has the same standing as the nominal `K/pi` branch

Relevant files:

- `overleaf_repo_git/main.tex:614-648`
- `overleaf_repo_git/report/20260310_dndeta_unfolding_update.md:135-142`

What is wrong:

- The analyzer status note explicitly says the `p/\pi` `dN_{ch}/d\eta` closure is
  still not at the same quality level as `K/\pi`.
- The main note's `p/\pi` section still says the branch uses the same correction
  strategy, unfolding setup, and uncertainty prescription, then presents the
  `dN_{ch}/d\eta` result without an explicit caveat.

Why this matters:

- This is a wording problem, not a missing file problem.
- The current note does not clearly signal that the `p/\pi` `dN_{ch}/d\eta`
  branch remains a weaker-status result.

Suggested fix:

- Add one explicit caveat sentence in the `p/\pi` section stating that the
  `dN_{ch}/d\eta` branch is shown as a parallel status result and still has
  weaker closure quality than the nominal `K/\pi` branch.

### 5. Low: one sentence is still too informal for a note-quality provenance statement

Relevant file:

- `overleaf_repo_git/main.tex:452`

What is wrong:

- The sentence
  `This is done with Nishant efficiencies from Mar 4, 2026.`
  is conversational and underspecified.

Why this matters:

- It is not clear whether this means:
  - efficiencies provided externally,
  - efficiencies regenerated from a specific script,
  - or efficiencies imported from a fixed tagged production set.

Suggested fix:

- Replace it with a provenance sentence that identifies the artifact more
  precisely, for example by naming the efficiency set, the producing workflow,
  and the date in a note-style format.

### 6. Low: the appendix section title still triggers hyperref PDF-string warnings

Relevant files:

- `overleaf_repo_git/main.tex:1012`
- `overleaf_repo_git/main.log`

What is wrong:

- The section title
  `External \(K/\pi\) and \(K^0_S/\pi\) Tables vs Midrapidity \(dN_{\mathrm{ch}}/d\eta\)`
  contains raw math in the heading.
- The current build emits hyperref PDF-string warnings on this line.

Why this matters:

- The PDF still builds, but the bookmark metadata is noisy and LaTeX warns every
  time.

Suggested fix:

- Wrap the title in `\texorpdfstring{...}{...}` with a plain-text bookmark
  version.

### 7. Medium: the note should be audited against the full Electron-Positron Alliance collaboration paper list, not only the single EPA citation currently present locally

Relevant files:

- `overleaf_repo_git/main.tex:105`
- `overleaf_repo_git/main.tex:117`
- `overleaf_repo_git/main.tex:125`
- `overleaf_repo_git/main.tex:838-839`

What I verified locally:

- The current note cites one Electron-Positron Alliance paper through
  `eea_delphi_note_2025`.
- I do not see additional EPA collaboration entries cited or discussed in the
  current note source.

Reviewer request:

- Cross-check the note against the full Electron-Positron Alliance collaboration
  list on the INSPIRE page provided separately.
- Make sure every EPA collaboration paper that is relevant background,
  methodology, validation, or comparison context for this note is either:
  - explicitly cited and briefly discussed, or
  - explicitly judged not relevant and therefore omitted on purpose.

Why this matters:

- If the EPA collaboration now has multiple public papers, citing only the older
  open-data benchmark note may under-document the analysis context and related
  methodology.

Suggested fix:

- Add a short citation audit pass against the full INSPIRE EPA list and update
  the introduction or methodology sections accordingly.

## Reference check summary

The current note build passes the hard reference checks:

- `latexmk` completes successfully on `overleaf_repo_git/main.tex`
- no undefined `\ref` warnings were reported in `main.log`
- no undefined citation warnings were reported in `main.log`
- the direct label sweep found `0` missing label-based references

So the remaining issues in this pass are mostly:

- stale wording
- brittle hard-coded numeric references
- note/code mismatch in one figure caption
- note-quality phrasing improvements

## Overall review status

The note is in much better shape on hard references than on the previous passes:
I did not find broken LaTeX references or missing citation resolutions in the
current build.

The remaining work is mainly editorial and consistency cleanup, with one
important exception: the stale `dN_{ch}/d\eta` workflow sentence and the
refolding-caption versus plotting-macro mismatch should both be fixed before the
note is treated as fully synchronized.
