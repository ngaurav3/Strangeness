2026-03-10
Iteration 3

# Reviewer Report

Author: Yen-Jie Lee and OpenAI

## Scope

This review re-checks the iteration 1 findings against the current code, outputs,
and note after the analyzer-side refresh and the PID scale-factor update.

The main questions for this pass were:

- whether the iteration 1 comments were addressed
- whether the active outputs now follow the unfolding-based `N_tag` builder
- whether the new PID scale-factor values are propagated consistently

## Findings

### 1. Medium: the main iteration 1 comments appear to be addressed in the active `N_tag` chain, but the `dNch/deta` finalizer still reuses the nominal closure correction for all variations

Relevant files:

- `build_systematics_v3_with_unfolding.py:141-194`
- `build_systematics_v3_with_unfolding.py:371-399`
- `finalize_dndeta_systematics.py:75-78`
- `finalize_dndeta_systematics.py:110-122`
- `overleaf_repo_git/main.tex:745-749`

What was checked:

- The current `N_tag` v3 builder now reconstructs the nominal result through
  `build_bundle(...)` and `apply_closure_and_pid(...)`, rather than rescaling the
  old reco-level table.
- The generated `output/systematics_20260306_v3/systematics_table_v3.txt`
  matches the current builder numerically for the nominal central values and
  statistical uncertainties.
- The Bayes unfolding code now assigns bin errors in the active implementation.
- The residual term is now converted to an absolute uncertainty in both the code
  and the note.
- The stale figure reference in Section 3.1 was updated from Figs. 1--2 to
  Figs. 2--3.

Remaining issue:

- In `finalize_dndeta_systematics.py`, every variation histogram is corrected with
  the nominal closure histogram from `nominal_unfold_dndeta.root`.
- This means the variation envelope is evaluated after applying the nominal
  residual correction, not the variation-specific residual correction.

Why this matters:

- The note already documents that the `dNch/deta` branch is still a status-level
  chain using the nominal response set for all variations.
- However, the current caveat does not explicitly say that the residual closure
  correction is also borrowed from the nominal variation.

Conclusion:

- The iteration 1 high-severity `N_tag` issues are fixed in the active chain.
- The remaining methodological caveat is now concentrated in the `dNch/deta`
  finalizer, which is still partly nominalized across variations.

### 2. Low: one note notation typo remains in the `dNch/deta` systematics description

Relevant file:

- `overleaf_repo_git/main.tex:726-728`

What is happening:

- The note text says
  `h\)ClosureBayes\(_{dN/d\eta}\)`
  instead of a clean histogram name or prose description.

Why this matters:

- This does not appear to change the analysis logic.
- It is still visible documentation noise in the systematics section.

Conclusion:

- The active note is much closer to consistency now, but this local notation bug
  should still be cleaned up.

## Verification summary

The following iteration 1 points were re-checked and appear addressed:

1. `N_tag` v3 central values are now built from the unfolding-based bundle logic.
2. `N_tag` statistical uncertainties are now propagated in the active implementation.
3. The residual term is now treated as an absolute uncertainty in both code and note.
4. The stale Section 3.1 figure references were updated.

## PID scale-factor update status

The new PID scale-factor values are propagated consistently in the active chain.

Checked values:

- `SF_K = 0.9694`
- `SF_PI = 0.9790`
- `sigma(SF_K) = 0.026703`
- `sigma(SF_PI) = 0.038114`

Relevant files:

- `build_systematics_v3_with_unfolding.py:10-15`
- `finalize_dndeta_systematics.py:10-15`
- `make_kpi_pid_sf_before_after_plots.py:10-12`
- `make_kpi_vs_dndeta_comparison_plots.py:14-19`
- `overleaf_repo_git/main.tex:418-425`
- `overleaf_repo_git/main.tex:678-689`

The generated `N_tag` and `dNch/deta` systematic tables are numerically consistent
with those updated scale factors.

## Assumptions and limits of this review

- This was a consistency review only; no new analysis jobs were launched in this pass.
- The review treats the active Python-based `N_tag` builder as authoritative for the
  current quoted result.
- Historical status-note files were not treated as authoritative if they still
  contained older scale-factor values.

## Recommended next actions

1. Decide whether the current `dNch/deta` treatment is acceptable as a status result or whether variation-specific closure corrections are now required.
2. If the status-level approximation is retained, state that explicitly in the note alongside the existing nominal-response caveat.
3. Fix the remaining notation typo in the `dNch/deta` systematics text.

## Overall review status

The main iteration 1 review comments are addressed in the active `N_tag` result path.

The current state looks substantially improved:

- unfolding-based `N_tag` result construction is in place
- statistical bars are no longer obviously borrowed from the old reco-level table
- the new PID scale-factor update is wired through the active scripts and the note

The remaining review concern is a narrower `dNch/deta` methodological caveat,
plus a small documentation typo.
