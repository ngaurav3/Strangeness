2026-03-12
Iteration 10

# Reviewer Report

Author: Yen-Jie Lee and OpenAI

## Scope

This pass checks whether the latest March 12 updates are propagated consistently across:

- the active unfolding macros
- the current result and comparison plots
- the synced analysis note
- the synced status-deck sources and workspace report copies

Mechanical checks completed:

- the workspace and Overleaf copies of `20260312_iteration10_reviewer_followup.md` are identical
- the workspace and Overleaf copies of `20260312_unfolding_checks_status_note.md` are identical
- the workspace and Overleaf copies of `20260312_complete_status_report_from_overleaf_note.{md,tex}` are identical
- no missing `\includegraphics` targets were found in `overleaf_repo_git/main.tex` or `report/20260312_complete_status_report_from_overleaf_note.tex`

## Findings

### 1. High: the March 12 toy-statistics update is not propagated to the `K/\pi` `dN_{ch}/d\eta` comparison/context figures, even though the note and slide material now describe those figures as part of the current status package

Relevant files:

- `overleaf_repo_git/main.tex:609`
- `overleaf_repo_git/main.tex:629`
- `overleaf_repo_git/main.tex:635`
- `overleaf_repo_git/main.tex:765`
- `report/20260312_complete_status_report_from_overleaf_note.tex:342-349`
- `make_kpi_vs_dndeta_comparison_plots.py:203-215`
- `make_kpi_vs_dndeta_comparison_plots.py:264-265`
- `make_world_ee_kpi_comparison.py:219-255`
- `finalize_dndeta_systematics.py:36-56`
- `finalize_dndeta_systematics.py:101-103`

What is happening:

- The updated final-result path does know about the toy study:
  - `finalize_dndeta_systematics.py` loads `result/20260312/reviewer_followup_validation/toy_coverage/toy_coverage_bins.csv`
  - it overrides the nominal bin errors before writing `systematics_dndeta_summary.root`
- The comparison/context plot scripts do not use that path:
  - `make_kpi_vs_dndeta_comparison_plots.py` clones `hRatioDataBayes_dNdEta`, `hRatioMcBayes_dNdEta`, and `hDataOverMcBayes_dNdEta` directly from `output/systematics_20260306_dndeta/nominal_unfold_dndeta.root`
  - `make_world_ee_kpi_comparison.py` does the same
  - neither script loads the toy-coverage CSV or the summary ROOT file with the overridden statistical errors
- This is not a small effect. In the current files:
  - the raw last-bin double-ratio error in `nominal_unfold_dndeta.root` is about `0.0495`
  - the March 12 toy-calibrated last-bin error in `systematics_dndeta_summary.root` is about `0.00274`

Why this matters:

- The final double-ratio figure can be consistent with the March 12 text.
- But the comparison figures used in the note and slides are still built from a different statistical treatment.
- In particular, the note now says the displayed bars are toy-calibrated in reader-facing captions, while the actual comparison/context figure producers still consume the raw Bayes errors.

Requested fix:

- Put all presented `K/\pi` `dN_{ch}/d\eta` figures on one statistical source of truth.
- The cleanest fix is to make the comparison/context plot scripts read the already-finalized summary objects, or at minimum apply the same `load_toy_stat_errors("dNdEta")` override before graph construction.
- If the March 12 toy calibration is meant to apply only to the double-ratio figure and not to the single-ratio DELPHI-vs-MC / DELPHI-vs-ALICE views, then the note captions at `main.tex:629` and `main.tex:635` should be downgraded to say only "statistical bars" rather than "toy-calibrated statistical bars."

### 2. High: the current `keepBins=8` status branch is not reproducible from the scripted defaults

Relevant files:

- `overleaf_repo_git/main.tex:268-270`
- `overleaf_repo_git/main.tex:419-423`
- `report/20260312_complete_status_report_from_overleaf_note.tex:181`
- `report/20260312_complete_status_report_from_overleaf_note.tex:200`
- `report/20260312_complete_status_report_from_overleaf_note.tex:406`
- `run_systematics_dndeta_unfolding.py:11-13`
- `run_systematics_dndeta_unfolding.py:209-213`
- `runDNdEtaUnfolding_BayesSVD.C:457-462`

What is happening:

- The note and status deck now state clearly that the active working choice is `keepBins=8`.
- But the main driver still defaults to:
  - `KEEPBINS_OVERRIDE = -1`
  - which means "use the macro's automatic choice"
- The macro's automatic choice is derived from `DetermineOverflowKeepBins(hRecoCountsFine, 100.0)`.
- For the current nominal reco-count histogram in `output/KtoPi-MC-Reco-Nominal.root`, that automatic rule gives `keepBins=10`, not `8`.
- Meanwhile, the committed `output/systematics_20260306_dndeta/nominal_unfold_dndeta.root` clearly has 8 visible bins, so the published status branch depends on an override or a manual step that is not encoded in the default workflow.

Why this matters:

- A user who reruns the documented driver from defaults will not reproduce the current March 12 branch.
- Worse, they can silently regenerate a different `dN_{ch}/d\eta` output while believing they reproduced the note.

Requested fix:

- Make `keepBins=8` explicit in the active driver that is supposed to reproduce the quoted status plots.
- At minimum, set the driver default to `KEEPBINS_OVERRIDE=8` for the current branch and document that choice in the workflow notes.
- Better still:
  - rename the active branch outputs to encode the tail choice, or
  - write the chosen `keepBins` value into the output ROOT metadata and printed tables
- Right now the documentation is explicit, but the reproduction path is not.

### 3. Medium: there are still two competing `dN_{ch}/d\eta` result-building paths that can write the same nominal outputs with different logic

Relevant files:

- `run_systematics_dndeta_unfolding.py:182-299`
- `finalize_dndeta_systematics.py:58-182`
- `finalize_dndeta_systematics.py:185-261`

What is happening:

- `run_systematics_dndeta_unfolding.py` still contains its own full summary-building path:
  - it writes `systematics_dndeta_summary.root`
  - it writes `systematics_dndeta_table.txt`
  - it writes `KtoPi_DoubleRatio_vs_dNdEta_with_Systematics.pdf`
- `finalize_dndeta_systematics.py` now writes those same nominal summary outputs using the updated March 12 logic:
  - variation-specific residual correction
  - toy-statistical override
  - current four-component systematic treatment
- The note and the March 12 reports only describe the updated logic, not the legacy path.

Why this matters:

- The repo currently stays consistent only if analysts remember which writer not to run.
- The old path can overwrite the same filenames with different assumptions.
- That is exactly the kind of ambiguity that breaks note/code consistency a week later.

Requested fix:

- Retire or rename the legacy writer, or make it call `finalize_dndeta_systematics.py` instead of producing its own competing summary outputs.
- The current active branch should have one authoritative builder for the files that the note and slides cite.

## Items That Do Look Consistent

- The March 12 workspace and Overleaf status notes are synchronized.
- The main note and the status deck now carry the same `keepBins=8` narrative and the same species-level tail caveat.
- The finalized `K/\pi` `dN_{ch}/d\eta` summary object in `output/systematics_20260306_dndeta/systematics_dndeta_summary.root` does use the March 12 toy-calibrated errors, and those errors match the values in `result/20260312/reviewer_followup_validation/toy_coverage/toy_coverage_bins.csv`.

